# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。
版本号格式 `主版本.次版本.修订号`，浏览器「扩展」页面显示的版本取自 `manifest.json`。

---

## [1.1.0] — 当前版本

在基础分屏对照翻译之上，补齐了「拿它真正读新闻」时缺的交互能力。

### 新增

- **译文栏链接可直接点击**：站内链接在当前标签页打开，站外链接自动新开标签页，
  `Ctrl` / 中键强制新开；页内锚点滚动原文栏；`javascript:` / `data:` / `blob:` / `file:`
  协议一律拒绝，并由 Service Worker 侧二次校验
- **四种分屏版面**：原文可在左 / 右 / 上 / 下。上下版面时主轴变纵向、分隔线自动转为横向可拖动，
  滚动同步与角标定位跟随主轴
- **译文独立小窗**：基于 Document Picture-in-Picture，把译文栏搬进可拖动的悬浮小窗，
  关掉小窗自动搬回原位并保留版面与比例（默认关闭，可在设置或状态条触发）
- **思考强度**：可选 `reasoning_effort`（低 / 中 / 高）；接口不支持该参数时
  自动去掉参数重试一次，不会因此翻译失败
- **悬停对照**：悬停译文显示原文气泡，并高亮原文栏对应段落
- **原文 / 译文角标**：两栏左上角标注，滚动后自动淡出

### 修复

- 译文栏此前屏蔽了全部点击事件，导致「译文里的链接点不动」，只能点原文栏
- 拖动分隔线时比例换算与滚动轴未跟随版面方向
- 页面尺寸变化后角标位置未重算

### 工具

- 新增 `docs/release-notes.md`：Release 说明模板与发布字段说明
- 新增 `docs/listing.md`：Edge / Chrome 扩展商店上架文案与权限说明表
- 自检从 107 项扩展到 **140 项**，端到端断言从 77 项扩展到 **131 项**

---

## [1.0.0] — 基础版本

首个可用版本，确立分屏对照翻译的核心实现。

### 新增

- 自动语言检测（Unicode 文字系统统计 + 拉丁语系停用词打分 + 简繁特征字判定）
- 左右分屏：原文栏放页面原始 DOM（原站 CSS 完全生效），译文栏为结构一致的深拷贝镜像
- 逐段文本回填：扫描时给文本节点包 `span[data-stt-id]`，镜像按同一 id 精确对应
- 批量翻译：按「段数 + 字符数」双阈值合批、同文去重、串行发送
- 统一走 OpenAI 兼容的 `/chat/completions`；内置 DeepSeek、MiniMax 预设，支持自定义
- 本地翻译缓存：按「服务商 + 模型 + 目标语言」哈希存入 `chrome.storage.local`
- 自动跳过代码块、命令行、URL、邮箱、纯数字、`translate="no"`、`contenteditable`、隐藏元素
- 顶部提示条（智能提示 / 完全自动 / 仅手动 / 关闭四种自动化程度）
- 工具栏弹窗、设置页、右键菜单（分屏翻译此页面 / 翻译选中文本）
- 动态加载内容续翻（可选）、图片取字（可选多模态识别接口）
- 页面级字符预算，防止动态页面耗尽 API 额度

### 开发期修正的真实缺陷

- `document.createTreeWalker` 的 `FILTER_REJECT` 行为不可靠，导致一页只登记到 1 个文本段；
  改为自写递归遍历
- 克隆镜像发生在搬移 `body` 子节点之后，把刚建好的分屏容器自己也克隆了进去
- 提示条被克隆进译文栏并被当作正文翻译
- `flex-basis: var(--st-ratio) !important` 在部分 Chromium 上不随变量变化重新解析，
  比例锁死在 50%；改为运行时写内联像素级 flex 值，并把结果记录到 `data-stt-*` 属性
- 默认排除名单误伤 `localhost` / `127.0.0.1`，导致本地页面静默不翻译

---

## 未发布

- 暂无
